
function formatcusopr(input) {
    input.value = input.value.replace(/\D/g, ''); // Remove all non-numeric characters
    // input.value = input.value.replace(/[^\d\s]/g, '');   need space 

}

function cusoprcaps(input) {
    input.value = input.value.toUpperCase(); // Remove all non-numeric characters
}



